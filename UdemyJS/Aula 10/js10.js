// While e uma estrutura de repeticao e bloco de codigo enquanto a condicao do mesmo for valida. //

var x = 0;

while (x < 3) {
    alert (x);

    x = x + 1;
}

// O laco "for" executa PARA uma determinada condicao. //
var y = 0;

for (var y = 0; y < 3; y++) {
    alert (y);
}